apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - servicemonitor.yaml
  - prometheusrule.yaml

# AlertmanagerConfig와 Slack ExternalSecret은 Terraform
# infra/modules/addons/alertmanager.tf에서 단독 관리합니다.
