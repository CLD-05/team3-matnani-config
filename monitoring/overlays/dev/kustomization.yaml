apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: servicemonitor-patch.yaml
    target:
      kind: ServiceMonitor
      name: matnani-api

# AlertmanagerConfig와 Slack ExternalSecret은 Terraform에서 환경별로 관리합니다.
